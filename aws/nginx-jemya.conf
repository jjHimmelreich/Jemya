# HTTPS server (main configuration)
server {
    listen 443 ssl default_server;
    listen [::]:443 ssl default_server;
    server_name jam-ya.com www.jam-ya.com;
    
    # Self-signed SSL certificate (replaced by certbot/Let's Encrypt after DNS propagates)
    ssl_certificate /etc/nginx/ssl/jemya.crt;
    ssl_certificate_key /etc/nginx/ssl/jemya.key;
    
    # Modern SSL configuration
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-SHA256:ECDHE-RSA-AES256-SHA384;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    
    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_read_timeout 86400;
        proxy_send_timeout 86400;
    }
}

# HTTP server (redirect to HTTPS)
server {
    listen 80;
    listen [::]:80;
    server_name jam-ya.com www.jam-ya.com;
    
    # Redirect all HTTP traffic to HTTPS
    return 301 https://$host$request_uri;
}